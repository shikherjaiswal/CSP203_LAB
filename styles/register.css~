html{
	background:linear-gradient(to left,rgb(150,22,283),rgb(158,20,20)) no-repeat fixed ;
	margin:0; 
	border:0;
	overflow: auto;
	padding: 0;
	font-size: 25px; 
	font-family: Ubuntu,Helvetica Neue,Helvetica,Arial,sans-serif;
	font-weight:300;
	color:white;
}

body {
	text-align: center;
	position: relative;
	top : 140px;
}

table,th,td{
   font-size:25px;
   border: 4px solid black;
   border-collapse:collapse;
   top:30px;
   font-weight:500;
}


th,td{
	padding:20px;
	padding-top:5px;
	padding-bottom:5px;
}

table {
	position: relative;
	width: 100%;
	text-align: center;
	display: inline-table;
	height:380px;
}

#DailyMenu {
	width : 60%;
	height : 20%;
}


#registerBack {
	position: absolute;
	display:inline-block;
	border: 20px;
	border-radius: 20px; 
	background-color: black;
	opacity: 0.4;
	height: 370px; 
	max-width: 500px;
	width: 90%;
	min-width: 250px;
}


#register {
	border: 20px;
	border-radius: 20px; 
	position: relative;
	display: inline-block;
	max-width: 500px;
	width: 90%;
	min-width: 250px;
	text-align: center;
}


h1 {
	font-family: Ubuntu,sans-serif;
	color: white;
	padding-top: 15px; 
	margin: 0;
	margin-top:3px;
	border-bottom: 1px solid rgb(30,180,158);
	padding-bottom: 10px;	
	font-size: 35px;
	font-weight: 100;
}

input {
	margin: 5px;
	border:0px solid white;
	position: relative;
	max-width: 250px;
	width:80%; 
	padding: 8px;
	font-size: 70%;
	border-radius: 5px;
}

input:hover{
	border: 0px;
}

button{
	font-size: 18px;
	color:white;
	margin: 25px;
	width: 120px;
	background:linear-gradient(to left,rgb(0,122,150),rgb(28,100,170)) no-repeat fixed ;
	border: 0px;
	border-radius: 10px;
	height: 37px;
	transition-duration: 0.6s;
}

button:hover{	
	background: linear-gradient(to right,rgb(0,122,150),rgb(28,100,170)) no-repeat fixed ;
}

button span {
  cursor: pointer;
  display: inline-block;
  position: relative;
  transition: 0.5s;
}

button span:after {
  content: "\00bb";
  position: absolute;
  opacity: 0;
  top: 0;
  right: -20px;
  transition: 0.5s;
}

button:hover span {
  padding-right: 23px;
}

button:hover span:after {
  opacity: 1;
  right: 0;
}


a{
	display: inline-block;
	background: linear-gradient(to left,rgb(0,122,80),rgb(10,200,220)) no-repeat fixed ;
	padding: 30px;
	padding-top: 15px;
	padding-bottom: 15px;
	margin: 20px;
	border: 1px solid white;
	border-radius: 15px;
	text-decoration: none;
	color: white;
	width:250px;
	height:20px;
}

a:hover{
	background: white;
	color: rgb(0,100,255);
}


.switch {
  position: relative;
  display: inline-block;
  width: 60px;
  height: 34px;
}

.switch input {
	display:none; 
}

.slider {
  border: 1px;
  position: absolute;
  cursor: pointer;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: #ccc;
  -webkit-transition: .4s;
  transition: .4s;
}

.slider:before {
  position: absolute;
  content: "";
  height: 26px;
  width: 26px;
  left: 4px;
  bottom: 4px;
  background-color: white;
  -webkit-transition: .4s;
  transition: .4s;
}

input:checked + .slider {
  background-color: #2196F3;
}

input:focus + .slider {
  box-shadow: 0 0 1px #2196F3;
}

input:checked + .slider:before {
  -webkit-transform: translateX(26px);
  -ms-transform: translateX(26px);
  transform: translateX(26px);
}

/* Rounded sliders */
.slider {
  border-radius: 34px;
}

.slider:before {
  border-radius: 50%;
}

.home{
   background:rgb(10,120,221);
	position :fixed;
	top : 20px;
	left : 20px;
	padding: 2%;
	padding-top: 10px;
	padding-bottom: 10px;
	border: 0;
	margin: 0;
}

#header{
    display:block;
    position:fixed;
    color:white;
    background : rgb(10,100,120);
    top:0px;
    width:100%;
    right:0px;
    display:block;
    height:21%;
}

img{
    position:fixed;
    top:3px;
    height:20%;
    float:left;
    left : 10%;
}

h2{
    position:fixed;
    left:30%;
    font-size:3.5vw;
    font-weight:500;
    top:1%;
    height:17%;
}

.logout{
	position :fixed;
	top : 20px;
	right : 20px;
	padding: 0px;
	padding-top: 0px;
	padding-bottom: 0px;
    width:120px;
    height:30px;
}

.popup {
    position: relative;
    display: inline-block;
    cursor: pointer;
    -webkit-user-select: none;
    -moz-user-select: none;
    -ms-user-select: none;
    user-select: none;
}

/* The actual popup */
.popup .popuptext {
    visibility: hidden;
    width: 190px;
    	background:linear-gradient(to left,rgb(150,22,243),rgb(118,240,120)) no-repeat fixed ;
    color: #fff;
    text-align: center;
    border-radius: 6px;
    padding: 8px 0;
    position: absolute;
    z-index: 1;
    bottom: 125%;
    left: 50%;
    margin-left: -80px;
}

/* Popup arrow */
.popup .popuptext::after {
    content: "";
    position: absolute;
    top: 100%;
    left: 50%;
    margin-left: -5px;
    border-width: 5px;
    border-style: solid;
    border-color: rgb(150,122,283);
}

/* Toggle this class - hide and show the popup */
.popup .show {
    visibility: visible;
    -webkit-animation: fadeIn 0.1s;
    animation: fadeIn 0.1s;
}

/* Add animation (fade in the popup) */
@-webkit-keyframes fadeIn {
    from {opacity: 0;} 
    to {opacity: 1;}
}

@keyframes fadeIn {
    from {opacity: 0;}
    to {opacity:1 ;}
}
	
.numbers{
    width:50px;
    text-decoration:none;
    font-size:18;
    height:35px;
}

h2{
    font-size:50px;
}

.loginIndex{
    background:rgb(200,30,80);
}





